import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 10,
    width: '100%',
    height: 40,
    marginVertical: 4,
    padding: 12,
  },
});

export default styles;
